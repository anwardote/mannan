


/*
    
	function class_models(id){
       /* $('#ClassEditModal').modal({
			show: 'false' }); 
		
		
	$.ajax({
        url : "<?php echo site_url('configaration/class_edit/')?>/" + id,
        type: "GET",
        dataType: "JSON",
        success: function(data)
        {

            $('[name="class_codes"]').val(data.class_code);
			 $('##ClassEditModal').modal('show');
			/*
            $('[name="firstName"]').val(data.firstName);
            $('[name="lastName"]').val(data.lastName);
            $('[name="gender"]').val(data.gender);
            $('[name="address"]').val(data.address);
            $('[name="dob"]').datepicker('update',data.dob);
            $('#modal_form').modal('show'); // show bootstrap modal when complete loaded
            $('.modal-title').text('Edit Person'); // Set title to Bootstrap modal title
			

        },
        error: function (jqXHR, textStatus, errorThrown)
        {
            alert('Error get data from ajax');
        }
    });
	function config_update(){
		


	    $.ajax({
        url :  "<?php echo site_url('configaration/class_update/')?>/",
        type: "POST",
        data: $('#config_modal').serialize(),
        dataType: "JSON",
        success: function(data){
			
			
			});
		
		}


		
		}
		
*/
    
        
		 
